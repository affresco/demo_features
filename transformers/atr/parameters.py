# Default Bollinger bands time spans in minutes
ATR_SPANS = [10, 15, 30, 60, 120, 360, 720, 1440]

# Default columns mapipng
ATR_COLUMNS = {
    "high": "high",
    "low": "low",
    "close": "close",
}
